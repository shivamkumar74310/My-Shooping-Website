function orderItem(category, itemName, price) {
    alert(`You have ordered a ${category.toLowerCase()} - ${itemName} for $${price.toFixed(2)}`);
    // You can customize this function to perform other actions, e.g., updating a shopping cart.
}
